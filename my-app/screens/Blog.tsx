import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Blog = () => {
  return (
    <View>
      <Text>{2 >= 2}</Text>
    </View>
  );
};

export default Blog;

const styles = StyleSheet.create({});
