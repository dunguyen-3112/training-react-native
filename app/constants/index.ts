import { API_ENDPOINT, ROUTES } from './API';
import { COLORS, COLOR } from './color';
import {
  FONT_SIZES,
  FONT_SIZE_TYPE,
  FONT_WEIGHTS,
  FONT_WEIGHT_TYPE,
} from './Font';
import Categories from './Category';

export {
  COLORS,
  FONT_SIZES,
  FONT_SIZE_TYPE,
  FONT_WEIGHTS,
  FONT_WEIGHT_TYPE,
  COLOR,
  API_ENDPOINT,
  ROUTES,
  Categories,
};
